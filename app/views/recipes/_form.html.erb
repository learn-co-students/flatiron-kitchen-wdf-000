<%= form_for recipe do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>

Existing Ingredients:<br>
<%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %><br>

New Ingredient:<br>
<%= f.fields_for :ingredients, recipe.ingredients.build do |ingred| %>
	<%= ingred.text_field :name %>
	<% end %>
<%= f.submit %>
<% end %>