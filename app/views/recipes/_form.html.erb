<div style="width: 75%; margin: 0 auto; text-align: center;">
  <div class="form-wrap" style="border: 3px solid black; background: #108080;">
    <%= form_for recipe do |f|%>
    <p>
      <%= f.label :name %>
      <%= f.text_field :name %>
    </p>
    <p>
      <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %>
    </p>
    <p>
      Add Ingredient:
      <%= f.fields_for :ingredients, recipe.ingredients.build do |ingredient| %>
        <%= ingredient.text_field :name %>
        <%= ingredient.number_field :amount, value: 1, in: 0..999, step: 1 %>
      <% end %>
    </p>
      <%= f.submit %>
    <% end %>
  </div>
  <div><%= link_to "Back to Index", recipes_path %></div>
</div>
